#include <bits/stdc++.h>
using namespace std;
void reverse_recr(int i, vector<int> nums)
{
}
int main()
{
  int n;
  cin >> n;
  vector<int> nums(n);
  for (int i = 0; i < n; i++)
  {
    cin >> nums[i];
  }
  cout << endl;
  for (int i = 0; i < n; i++)
  {
    cout << nums[i] << " ";
  }
  return 0;
}